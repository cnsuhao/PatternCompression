#include_directories(${DERP_SOURCE_DIR}/code)
# add_sources(main.cpp)

# file( GLOB SRCS *.c *.h *.cpp *.hpp) 

# add_executable( ${ExeName} ${SRCS} )
 
# add_executable( ${ExeName} ${SRCS}  ${HEADERS})
